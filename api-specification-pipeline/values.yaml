domain: smongiar
system: default-application
component: default-service
groupid: com.redhat

git:
  host: github.com
  branch: main

apicurioRegistry:  registry-apicurio-registry.apps.cluster-example.com
mockRegistry: microcks-microcks.apps.cluster-example.com

image:
  registry: image-registry.openshift-image-registry.svc:5000
